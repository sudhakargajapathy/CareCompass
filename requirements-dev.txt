# Development and test dependencies.
# Kept out of requirements.txt so the Hugging Face Spaces image stays lean.
-r requirements.txt

pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
